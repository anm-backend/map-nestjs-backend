// import { IsNotEmpty, Min } from 'class-validator';

// export const IsNotEmptyF = (fieldName: string) =>
//   IsNotEmpty({ message: `${fieldName} không được bỏ trống` });

// export const MinF = (len: number, fieldName: string) =>
//   Min(len, { message: `${fieldName} có ít nhất ${len} Ký tự` });

// export class CreateBaseDto {}
